README

I wasn't able to get the algorithm running to the point where I was able to make a table of the data.
But I think I'm very close to the correct implementation.

The LocalSearchDriver class will allow you to run the program in the 3 different ways.